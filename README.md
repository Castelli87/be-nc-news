If you want clone and work in this project you need to create two .env files where you can store the connection with the two databases 
follow the example .env-example 
you can find the different names of the databases in the setup.sql file 